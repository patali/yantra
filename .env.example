# Server Configuration
PORT=3000

# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/yantra

# JWT Configuration
# IMPORTANT: Change this to a secure random string in production!
# Generate one with: openssl rand -base64 32
JWT_SECRET=dev-secret-key-change-in-production

# Environment
# Options: development, production, test
NODE_ENV=development

# Application URL
# Used in password reset emails and other system notifications
APP_URL=http://localhost:3000

# System Email Configuration
# These settings are used for password reset emails and other system notifications
# In development mode, emails are logged to console instead of being sent

# Email Provider: Choose "smtp" or "resend" (default: smtp)
SYSTEM_EMAIL_PROVIDER=smtp
SYSTEM_EMAIL_FROM=noreply@yantra.local
SYSTEM_EMAIL_FROM_NAME=Yantra

# Option 1: SMTP Settings (e.g., Gmail, SendGrid, AWS SES)
# Use this option if you have an SMTP server
# SYSTEM_EMAIL_SMTP_HOST=smtp.gmail.com
# SYSTEM_EMAIL_SMTP_PORT=587
# SYSTEM_EMAIL_SMTP_USER=your-email@gmail.com
# SYSTEM_EMAIL_SMTP_PASSWORD=your-app-password

# Option 2: Resend Settings (modern email API)
# Get your API key from: https://resend.com/api-keys
# Set SYSTEM_EMAIL_PROVIDER=resend to use Resend
# SYSTEM_EMAIL_RESEND_API_KEY=re_your_api_key_here
