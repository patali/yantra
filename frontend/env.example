# Port on which the web application will be accessible
# Default: 8080
WEB_PORT=8080

# API Backend URL for Vite proxy configuration
# This is used when running the backend in a separate container
# Default: http://localhost:3000
VITE_API_URL=http://localhost:3000

